import type { PageMeta } from '@/types'

export const notFoundSeo: PageMeta = {
  title: 'Page not found',
  description: 'The page you are looking for does not exist.',
  path: '/404',
  noindex: true,
}
